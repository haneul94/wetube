extends layouts/main

block content
    .form-container
        form(action=routes.join, method="post")
            input(type="text", name="name", placeholer="Full Name", required=true)
            input(type="email", name="email", placeholer="Emai", required=true)
            input(type="password", name="password", placeholer="Password", required=true)
            input(type="password", name="password2", placeholer="Verify Password", required=true)
            input(type="submit", value="Join Now")
        include partials/socialLogin
